name: ext_link_account_plugin
description: A new Flutter plugin project.
version: 0.3.2

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: ExtLinkAccountPlugin
      android:
        package: com.example.app.ext.link.ext_link_account_plugin
        pluginClass: ExtLinkAccountPlugin