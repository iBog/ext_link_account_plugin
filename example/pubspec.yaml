name: ext_link_account_plugin_example
description: Demonstrates how to use the ext_link_account_plugin plugin.
publish_to: 'none'
version: 0.3.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  ext_link_account_plugin:
    path: ../

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true

